# =============================================================================
# Lexicon Compile Service — Application Configuration
# Spring Boot 4.0.6 | JDK 21 | GCP Cloud Run
# com.gliwka.hyperscan 5.4.0-2.0.0 | GZIP request + response
# =============================================================================
# NOTE: Each profile section uses spring.config.activate.on-profile under ---
# There is exactly ONE top-level "spring:" block below.
# =============================================================================

spring:
  application:
    name: lexicon-compile-service
  profiles:
    active: local
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
  jackson:
    serialization:
      write-dates-as-timestamps: false
      indent-output: false
    deserialization:
      fail-on-unknown-properties: false
    default-property-inclusion: non_null
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  lifecycle:
    timeout-per-shutdown-phase: 15s

server:
  port: ${PORT:8080}
  shutdown: graceful
  compression:
    enabled: true
    min-response-size: 1024
    mime-types:
      - application/json
      - application/json;charset=UTF-8
      - text/plain
  tomcat:
    threads:
      max: 200
      min-spare: 10

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
  endpoint:
    health:
      show-details: always
      show-components: always
  info:
    env:
      enabled: true

info:
  app:
    name: Lexicon Compile Service
    version: 1.0.0
    engine: Hyperscan (com.gliwka.hyperscan 5.4.0-2.0.0)
    springBoot: "4.0.6"
    jdk: "21"
    compression: GZIP request + response

lexicon:
  hyperscan-version: 5.4.0-2.0.0
  compiler:
    max-terms-per-request: 1000
  upload:
    max-file-size: 10MB

logging:
  level:
    root: WARN
    com.db.macs3.ecomms.spectre: INFO
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"

---
# =============================================================================
# LOCAL profile
# =============================================================================
spring:
  config:
    activate:
      on-profile: local

server:
  port: 8080

logging:
  level:
    com.db.macs3.ecomms.spectre: DEBUG

---
# =============================================================================
# TEST profile
# =============================================================================
spring:
  config:
    activate:
      on-profile: test

server:
  port: 0
  compression:
    enabled: true
    min-response-size: 1

logging:
  level:
    com.db.macs3.ecomms.spectre: DEBUG

---
# =============================================================================
# CLOUD-RUN profile
# =============================================================================
spring:
  config:
    activate:
      on-profile: cloud-run

server:
  port: ${PORT:8080}
  compression:
    enabled: true
    min-response-size: 1024
  tomcat:
    threads:
      max: 200
      min-spare: 20

lexicon:
  compiler:
    max-terms-per-request: 2000

logging:
  level:
    root: WARN
    com.db.macs3.ecomms.spectre: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} %5level [%thread] %logger{36} - %msg%n"
